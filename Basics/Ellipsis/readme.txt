Ellipsis dir:

wrote a function SecondMax() that uses an ellipsis parameter that takes in a series of ints and finds the second most maxiumum number. 

Parse dir: 

wrote a function to read in command line arguments and determine if the arguments are valid according to predetermined formatting 
("parse <option> <option>" where option is either "-t CHUNK_TYPE" or "-r CHUNK_NAME" where CHUNK_TYPE must belong to the enum of the same name, 
and CHUNK_NAME must be a max 19 character name. 

